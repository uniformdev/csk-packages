import { FlexItemParameters, FlexItemSlots } from '@/new-components/canvas/FlexItem';
import { ComponentProps } from '@/types/canvasTypes';

export type FlexItemProps = ComponentProps<FlexItemParameters, FlexItemSlots>;

export { default } from './flex-item';
